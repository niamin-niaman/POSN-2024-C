#include <stdio.h>

int main() {

  // int i = 0;
  // printf("i : %d\n",i);

  // i++;
  // printf("i : %d\n",i);

  // i++;
  // printf("i : %d\n",i);

  // i++;
  // printf("i : %d\n",i);

  // i++;
  // printf("i : %d\n",i);

  // int i ;
  // for (i = 1; i < 5 ; i*=3) {
  // // 1. check
  // 2. do
  // 3. ++

  // int i = 5; // initial
  // while (i > 1) { // condition
  //   i--; // increment
  //   printf("i : %d\n", i);
  // }

  // int i = 0;
  // do {
  //   i++;
  //   printf("i : %d\n", i);
  // } while (i < 5);


  int flag = 1 ;

  while(flag != 0){
    printf("enter flag : ");
    scanf("%d",&flag);
  }

  return 0;
}