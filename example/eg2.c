#include <stdio.h>

// ฟังก์ชันสำหรับคำนวณค่าเฉลี่ย
// double calculateAverage(int numbers[], int count) {
//     int sum = 0;

//     // หาผลรวมของตัวเลข
//     for (int i = 0; i < count; i++) {
//         sum += numbers[i];
//     }

//     // คำนวณค่าเฉลี่ย
//     double average = (double)sum / count;
//     return average;
// }

// // int main() {
// int benefit2(){
//     int numArray[] = {5, 10, 15, 20, 25};
//     int length = sizeof(numArray) / sizeof(numArray[0]);

//     // เรียกใช้ฟังก์ชันเพื่อคำนวณค่าเฉลี่ย
//     double result = calculateAverage(numArray, length);
//     printf("ค่าเฉลี่ย: %lf\n", result);
  
//     int numArray2[] = {10, 15, 20, 25, 30};
//     int length2 = sizeof(numArray2) / sizeof(numArray2[0]);

//     // เรียกใช้ฟังก์ชันเพื่อคำนวณค่าเฉลี่ย
//     double result2 = calculateAverage(numArray2, length2);
//     printf("ค่าเฉลี่ย 2: %lf\n", result2);
    
//     return 0;
// }