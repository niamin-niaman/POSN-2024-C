// โจทย์: ให้กำหนดอาร์เรย์ของตัวเลข แล้วให้เขียนโปรแกรมเพื่อหาค่าสูงสุดและต่ำสุดในชุดข้อมูลนี้.

#include <stdio.h>

// int main() {
int exercise_extra() {

    int data[] = {12, 45, 9, 67, 34, 2, 56, 23, 88, 43};
    int n = sizeof(data) / sizeof(data[0]);

    return 0;
  }