// ให้เขียนโปรแกรมที่รับความยาวด้านของรูปสี่เหลี่ยมจตุรัส (เป็นจำนวนเต็ม) จากผู้ใช้ แล้วคำนวณและแสดงผลลัพธ์พื้นที่ของรูปสี่เหลี่ยมจตุรัสนั้นออกทางหน้าจอ.
#include <stdio.h>

int main()
{

  return 0;
}
