#include <stdio.h>

int main() {
    // ประกาศและกำหนดค่าให้กับตัวแปรประเภท char
    char myChar;

    // กำหนดค่าให้กับตัวแปร myChar
    myChar = 'FAMIN';

    // แสดงค่าของตัวแปร myChar บนหน้าจอ
    printf("ค่าของตัวแปร myChar: %c\n", myChar);

    return 0;
}
