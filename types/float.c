#include <stdio.h>

int main() {
    // ประกาศตัวแปรชนิด float
    float myFloat;

    // กำหนดค่าให้กับตัวแปร myInteger
    myFloat = 1.234;
  
    // แสดงค่าของตัวแปร myFloat บนหน้าจอ
    printf("ค่าของตัวแปร myFloat คือ: %.1f\n", myFloat);

    // see more "%.2f" in keyword "output formatting in c"

    return 0;
}
