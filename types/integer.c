#include <stdio.h>

int main(void) {
  // ประกาศตัวแปรชนิด integer
  int myInteger;

  // กำหนดค่าให้กับตัวแปร myInteger
  myInteger = 42;

  // แสดงผลลัพธ์บนหน้าจอ
  printf("ค่าของตัวแปร myInteger คือ: %d\n", myInteger);
  
  return 0;
}