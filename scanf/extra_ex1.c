// คำนวณพื้นที่สี่เหลี่ยมจตุรัส โดยรับความยาวด้านจากผู้ใช้ และแสดงผลลัพธ์

#include <stdio.h>
int main() {
int lon;
  printf("โปรดป้อนความยาวด้านของสี่เหลี่ยมจตุรัส:");
  scanf("%d",&lon);
  
  printf("พื้นที่ของรูปสี่เหลี่ยมจตุรัสคือ: %d ตารางหน่วย\n", lon*lon);
  return 0;
}